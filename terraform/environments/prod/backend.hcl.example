# Copy to backend.hcl (gitignored) and fill in your state bucket, created by
# `terraform/bootstrap`. Then run: terraform init -backend-config=backend.hcl
bucket       = "autocare-terraform-state-<ACCOUNT_ID>"
key          = "prod/terraform.tfstate"
region       = "us-east-1"
encrypt      = true
use_lockfile = true
